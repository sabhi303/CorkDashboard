mixin card-sub-home(options = {id: '', title: 'Title', icon: '', description: 'Description', links: []})
  .sub-home__card
    .card__header
      h1.card__title= options.title
    .card__body!= options.description
    .card__links
      each l in options.links
        a(href=l.href)= l.name
